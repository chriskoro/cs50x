#include <stdio.h>
#include <cs50.h>

int main(void)
{
   //printf("hello, world\n");
   /*
   for (int i = 0; i < 10; i++)
   {
      int* a = malloc(123423);
      int addr = (int)a;
      printf("%i\n", addr);
   }
   */
   
   /*
   char *a = malloc(5);
   a[1000] = 'n';
	printf("%c", a[1000]);
	free(a);*/
   
   /***
   float a = 1/2;
   printf("%.2f", a);
   //prints 0.00, this would have printed 0.50 in python
   ***/
 

}

